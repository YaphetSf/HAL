<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>en</string>
	<key>CFBundleDisplayName</key>
	<string>HAL</string>
	<key>CFBundleExecutable</key>
	<string>HAL_input</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<key>LSMinimumSystemVersion</key>
	<string>$(MACOSX_DEPLOYMENT_TARGET)</string>
	<key>NSPrincipalClass</key>
	<string>NSApplication</string>

	<!-- LSUIElement, not LSBackgroundOnly: matches Apple's own IMK input methods, and
	     LSBackgroundOnly would forbid the candidate panel in M4. -->
	<key>LSUIElement</key>
	<string>1</string>

	<!-- D15 -->
	<!-- Must be "<bundle id>_Connection": that is the mach service name IMKServer actually
	     registers, and the system looks the input method up by it. A different name here
	     registers fine and even lets the source be added, but no client can ever connect,
	     so the input method silently cannot be switched to. -->
	<key>InputMethodConnectionName</key>
	<string>com.hal.inputmethod.HAL_Connection</string>
	<key>InputMethodServerControllerClass</key>
	<string>HAL_input.InputController</string>
	<key>TISInputSourceID</key>
	<string>com.hal.inputmethod.HAL</string>
	<key>TISIntendedLanguage</key>
	<string>zh-Hans</string>
	<key>tsInputMethodIconFileKey</key>
	<string>HALMenuIcon.tiff</string>

	<!-- Both scripts, because both are true since D11: HAL types Chinese, and in English mode
	     it hands every printable key to the app. TIS derives ASCII capability from this, so with
	     Latn declared HAL is what TISCopyCurrentASCIICapableKeyboardInputSource answers with.
	     Hans and Latn are ISO 15924 codes; the earlier zHans was invalid. -->
	<key>tsInputMethodCharacterRepertoireKey</key>
	<array>
		<string>Hans</string>
		<string>Latn</string>
	</array>
	<key>TISIconIsTemplate</key>
	<true/>
</dict>
</plist>
